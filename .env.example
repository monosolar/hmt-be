# Database connection string for PostgreSQL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# Example: postgresql://postgres:password@localhost:5432/meeting_scheduler?schema=public

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/meeting_scheduler?schema=public"

# Server configuration (optional)
PORT=3001
NODE_ENV=development
